;(function () {
    'use strict';

    declare('Config: Hash.config class', function () {
        it('config class should class `HashConfig`', function () {
            assert.strictEqual(Hash.config().constructor.name === 'HashConfig');
        });

        declare('.set', function () {
            it('', function () {

            });
        });

    });
})();